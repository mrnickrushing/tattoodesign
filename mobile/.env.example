# Base URL of the deployed web app's /api/generate endpoint (see ../src/app/api/generate).
# Defaults to the Railway deployment baked into src/lib/api.ts if unset.
EXPO_PUBLIC_API_BASE_URL=https://web-production-523e7.up.railway.app
